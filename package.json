{
  "name": "sinif-asistani",
  "version": "1.0.4",
  "description": "Sınıf Asistanı - Dijital Öğretmen Yardımcısı",
  "scripts": {
    "tauri": "tauri",
    "sync": "rsync -av --delete --exclude='*.DS_Store' index.html css js manifest.json sw.js icons tanitim vercel.json dist/",
    "dev": "npm run sync && tauri dev",
    "build": "npm run sync",
    "build:tauri": "npm run sync && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  }
}
